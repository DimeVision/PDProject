server:
  port: 0

spring:
  datasource:
    url: jdbc:postgresql://localhost:5444/Startup
    username: dimevision
    password: Admin15913371Fommkaa31

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
      default-zone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
